{
  "buildCommand": "cd ../.. && npm install && npm run build:shared && npm run build:web",
  "outputDirectory": "dist",
  "installCommand": "echo 'Skipping install, handled in buildCommand'",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
